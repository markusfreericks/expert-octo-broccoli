# an example for a specification document

the document is object of type "ExampleResult"
an ExampleResult MUST contain a field "list" that contains a list of "ExampleNode" elements
an ExampleNode MUST contain a nonempty string field "id"

